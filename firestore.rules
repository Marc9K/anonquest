rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    match /surveys/{surveyId} {
      allow read: if request.auth != null
        && request.auth.token.email in resource.data.participants;

      allow read, write, update: if request.auth != null
        && request.auth.token.email == resource.data.ownerEmail;
      
      allow create: if request.auth != null;

      match /questions/{questionId} {
        allow read: if request.auth != null
          && request.auth.token.email in resource.parent.data.participants;

        allow create: if request.auth != null;

        allow read, write, delete, update: if request.auth != null
          && request.auth.token.email == resource.parent.data.ownerEmail;

        match /answers/{answerId} {
          allow create: if request.auth != null
            && request.auth.token.email == request.resource.parent.data.ownerEmail;

          allow delete: if request.auth != null
            && request.auth.token.email == resource.parent.parent.data.ownerEmail;

          allow read, write, update: if request.auth != null 
        }
      }
    }
  }
}